/**
 * Internal implementation of MAPI. <strong>Not part of the public API</strong>:
 * everything in this package may change or be removed at any time without
 * notice. Mod consumers must only depend on {@code dev.example.mapi.api}.
 *
 * <p>Loader modules (fabric/, neoforge/) implement the interfaces in this
 * package to adapt their platform to MAPI. This package must never import
 * loader classes.
 */
package dev.example.mapi.internal;
